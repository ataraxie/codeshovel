CodeShovel Change Log

CodeShovel 0.1.2 (2018-07-26)
    * Add support to set the OUTPUT_DIR using an env var (fallback to default location if not set)
    * Update the Dockerfile to use the OUTPUT_DIR

CodeShovel 0.1.1 (2018-07-25)
    * Specify execution parameters in MiningTestJava to use env vars
    * Containerize MiningTestJava
        * Add Dockerfile
        * Update pom.xml to (1) emit test files in a jar (see <http://maven.apache.org/plugins/maven-jar-plugin/examples/create-test-jar.html>)
          and (2) include dependencies in the jar (see <https://stackoverflow.com/a/1729094>)

CodeShovel 0.1.0 (2018-07-16)
    * First beta release (for Nick)